<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:paddingStart="@dimen/folder_pill_padding_sides"
    android:paddingEnd="@dimen/folder_pill_padding_sides"
    android:layout_marginStart="@dimen/folder_pill_margin_left"
    android:foreground="@drawable/btn_effect"
    android:clickable="true"
    android:id="@+id/container">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="@dimen/folder_pill_icon_size"
            android:layout_height="@dimen/folder_pill_icon_size"
            android:src="@drawable/icon_folder_pill"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Title"
            android:textSize="@dimen/folder_pill_text"
            android:textColor="@color/pill_text"
            android:layout_marginStart="@dimen/folder_pill_text_margin_left"
            android:gravity="center_vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/icon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:ignore="HardcodedText" />
</androidx.constraintlayout.widget.ConstraintLayout>