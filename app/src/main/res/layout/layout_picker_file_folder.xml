<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/list_item_padding_top_bottom"
    android:paddingEnd="@dimen/list_item_padding_top_bottom"
    android:foreground="@drawable/list_item_effect"
    android:clickable="true"
    android:id="@+id/container">

        <ImageView
            android:id="@+id/icon"
            android:layout_width="@dimen/folder_icon_size"
            android:layout_height="@dimen/folder_icon_size"
            android:src="@drawable/icon_folder"
            android:layout_marginStart="@dimen/tool_bar_extra_padding_sides"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/name"
            android:layout_width="0dp"
            android:layout_height="@dimen/folder_list_height"
            android:text="Title"
            android:textSize="@dimen/folder_text"
            android:textColor="@color/folder_text"
            android:layout_marginStart="@dimen/folder_text_margin_left"
            android:background="@drawable/folder_item_divisor_horizontal_bottom"
            android:gravity="center_vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/icon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:ignore="HardcodedText" />
</androidx.constraintlayout.widget.ConstraintLayout>